{
  "name": "heatmap-dashboard",
  "version": "0.1.0",
  "description": "Modular heatmap simulation dashboard",
  "type": "module",
  "scripts": {
    "dev": "echo \"Run a static server to serve index.html\"",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
